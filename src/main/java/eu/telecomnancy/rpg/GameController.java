package eu.telecomnancy.rpg;

import java.util.HashMap;

public class GameController {

    private int id = 9;
    /* 
    public HashMap<String, Team> teams=new HashMap<String, Team>();

    public void combat(GameCharacter character1, GameCharacter character2) {
        int attack=character1.attack();
        int defense=character2.defense();
        if (attack>defense && attack-defense>0) {
            character2.applyDamage(character1.damage(attack-defense));
        }
    }

    // Organiser le combat jusqu'à ce que tous les membres d'une équipe soient hors de combat
    public void combatTeam(Team team1, Team team2) {
        // TODO Auto-generated method stub
        throw new UnsupportedOperationException("Unimplemented method 'combatTeam'");
    }

    public Team addTeam(String name, Team team) {
        Team t=new Team(name);
        teams.put(name, t);
        return t;
    }

    public void removeTeam(String name) {
        teams.remove(name);
    }

    public Team getTeam(String name) {
        return teams.get(name);
    }

    */

}